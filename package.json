{
  "name": "semipro-ai",
  "version": "1.0.0",
  "description": "Semipro AI - Full Stack Application",
  "scripts": {
    "build": "cd app && npm run build",
    "start": "cd backend && npm start",
    "dev": "./start.sh",
    "install:all": "cd app && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@fal-ai/client": "^1.9.1"
  }
}
